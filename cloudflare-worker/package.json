{
  "name": "parlance-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --exclude '**/*.e2e.test.js'",
    "test:e2e": "RUN_E2E=1 vitest run tests/realLifeTips.e2e.test.js",
    "test:unit": "vitest run tests/realLifeTips.unit.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
